dataset:
  name: "cifar10"
  batch_size: 128

ebm:
  energy_dim = 100
  leakyrelu_leak = 0.1
  p0_stddev = 1.0
  ula_eta = 0.16
  ula_numsteps = 60

gen:
  energy_dim = 100
  img_channels = 3
  image_res = 32
  leakyrelu_leak = 0.2
  gaussian_stddev = 0.3
  ula_eta = 0.01
  ula_numsteps = 20

model:
  z_dim = 80
